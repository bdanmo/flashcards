doctype html
html(lang="en")
  head
    title Flashcards
  body
    header
      h1 Flashcards
    section#content
      h2= prompt
      p
        i Hint: #{hint}
      img(src=`${img}`, alt=`Hey ${name}`)
    footer
      p An app to help you study.